interface:
  display_name: "Code Scenario Vscode"
  short_description: "Repo-specific patterns and workflows for code-scenario-vscode"
  default_prompt: "Use the code-scenario-vscode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true